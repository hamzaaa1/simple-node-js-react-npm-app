pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'npm install'  // This step installs the required dependencies for your Node.js and React application
            }
        }
    }
}
